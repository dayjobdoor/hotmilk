# CI + publish moved to publish.yml (Trusted Publisher expects that filename).
name: CI (deprecated)

on:
  workflow_dispatch:

jobs:
  redirect:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "::error::CI runs in publish.yml on push to main."
          exit 1
