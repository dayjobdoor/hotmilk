# Publish moved to ci.yml (job: publish, needs: test).
# Update npm Trusted Publisher: workflow file publish.yml → ci.yml
name: Publish Package (deprecated)

on:
  workflow_dispatch:

jobs:
  redirect:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "::error::Publish runs in ci.yml after test passes on push to main."
          echo "Update npm Trusted Publisher workflow file: publish.yml → ci.yml"
          exit 1
